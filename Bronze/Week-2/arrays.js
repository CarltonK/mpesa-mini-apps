const numberArrays = [1,2,3,4,5];

// Methods
// 1. Length
// This is a comment
const length = numberArrays.length;
console.log('The array has',length,'items');

// 2. Push
const push = numberArrays.push(6);
console.log('The array has',push,'items');

// 3. Pop
const pop = numberArrays.pop();
console.log('The element that has been removed is',pop);